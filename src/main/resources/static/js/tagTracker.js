export class TagTracker {
  constructor(id = null, tags, sportsbooks, statuses) {
    (this.id = id),
      (this.tags = tags),
      (this.sportsbooks = sportsbooks),
      (this.statuses = statuses);
  }
}
